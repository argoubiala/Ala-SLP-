// Ala SLP Activities — Supabase project connection
//
// The anon/public key below is designed to be used in client-side code
// (it's what your app's database Row Level Security policies are built to
// work with) — it is NOT the same as the secret/service_role key, and it's
// fine for it to live in this file.
//
// If you ever need to point this app at a different Supabase project,
// just replace the two values below with the new project's values from
// Settings → Data API (URL) and Settings → API Keys (anon/publishable key).

const SUPABASE_URL = "https://wqxxsfrnmcazdsxkuhmm.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_LEh3ec_tRx-swKm2Yh_f-g_i8PkS3SU";
